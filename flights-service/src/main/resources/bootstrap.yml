spring:
  application:
    name: flights-service
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:8888

#---
#spring:
#  profiles: cloud
#  cloud:
#    config:
#      uri: ${vcap.services.config-server.credentials.uri}
